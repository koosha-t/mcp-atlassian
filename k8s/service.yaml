apiVersion: v1
kind: Service
metadata:
  name: mcp-atlassian
  namespace: ${NAMESPACE}
  labels:
    app: mcp-atlassian
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: mcp-atlassian
